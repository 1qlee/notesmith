import { createContext } from "react"

export const EditorContext = createContext() // provides context values
export const EditorDispatchContext = createContext() // provides the function that lets components dispatch actions